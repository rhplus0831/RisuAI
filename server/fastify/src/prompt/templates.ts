/**
 * Phase 7 prompt template cards: chat ranges, cache markers, systemized
 * chat, position slots.
 *
 * Browser source to port:
 *   - `src/ts/process/templates/templates.ts`
 *   - `src/ts/process/templates/templateCheck.ts`
 *   - `src/ts/process/templates/chatTemplate.ts`
 *   - `src/ts/process/promptAssembly/systemizeChat.ts`
 */

export interface TemplateCardContext {
  // Phase 7-N: extend with template + chat-range + cache-marker context.
}

export async function renderTemplateCards(): Promise<TemplateCardContext> {
  throw new Error('phase-7 template card rendering not yet implemented')
}
