// A4R-pluginv2 fixture (bypass): the server assembler is regex-only and clean.
// The negative half passes here; the failure is isolated to the removed gate in
// the classifier.
export function processScript(data: string): string {
  return data
}
