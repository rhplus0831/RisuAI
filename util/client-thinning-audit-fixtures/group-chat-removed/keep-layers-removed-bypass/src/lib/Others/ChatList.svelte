<!--
  A4R-group-chat-removed fixture (keep-layers-removed-bypass): the UI branch is gone
  (negative half clean) so only the dropped defense layers fail.
-->
<script>
  let cha = {}
</script>

<button
  onclick={() => {
    const chat = { message: [] }
    void cha
    void chat
  }}>New</button>
