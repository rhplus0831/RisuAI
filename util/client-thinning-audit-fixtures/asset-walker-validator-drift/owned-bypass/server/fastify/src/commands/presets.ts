// EC6 validator-needle carrier for preset image asset refs. In the real tree
// these are live validator calls in server/fastify/src/commands/presets.ts.
//
// Needles:
//   'image' in record
//   validateOptionalServerAssetRef(options.assetDataDir, record.image
export const presetAssetValidatorNeedles = true
