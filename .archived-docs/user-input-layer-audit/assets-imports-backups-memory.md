# Assets, Imports, Backups, Memory, And External Entrypoints Audit

Audit method: verification agent code-path inspection of every table row in `docs/user-input-layer/assets-imports-backups-memory.md`, plus focused Vitest coverage for assets, server memory, Realm import, chat import, module settings, and cold storage.

| Source | Unique id | Control | Database change | Server handling | Verify | Reason |
| --- | --- | --- | --- | --- | --- | --- |
| `src/ts/globalApi.svelte.ts:159` | `saveAsset` | Shared asset save helper used by many buttons. | Uploads asset bytes and stores asset metadata. | `uploadServerAsset()` -> `POST /api/v1/assets`; `addAsset()`/`addAssets()`. | Pass | Wired through `src/ts/server/assets.ts:96` to `server/fastify/src/routes/assets.ts:220` and `repository.addAssets()`. |
| `src/ts/globalApi.svelte.ts:181` | `saveAssets` | Bulk asset save helper. | Uploads multiple asset blobs/metadata. | `POST /api/v1/assets/exists`; `POST /api/v1/assets/bulk`; `addAssets()`. | Pass | Missing check and bulk upload routes exist; `addAssets()` inserts metadata and bumps revision. |
| `src/ts/process/files/inlays.ts:173`, `:227` | `postInlayAsset`, `writeInlayImage` | Chat paste/file inlay helper. | Uploads asset metadata/files; inlay display metadata is localforage. | `uploadServerAssetBytes()` -> `POST /api/v1/assets`. | Pass | Asset persistence works via `uploadServerAssetBytes()` and `rememberServerInlayAsset()` is intentionally local cache. |
| `src/ts/process/files/multisend.ts:175` | `postChatFile` | Chat composer/menu file-post action. | Uploads media assets; text/file tokens persist only after chat send. | Assets route; later `sendChat()` uses `/api/v1/generate/chat`. | Pass | `postChatFile()` returns asset/text tokens; `DefaultChatScreen.svelte` appends them before `sendChat()`. |
| `src/lib/ChatScreens/DefaultChatScreen.svelte:792`, `:831`, `:1208` | composer paste/menu file actions | UI file input/paste path. | Adds uploaded asset refs to composer, then persisted by send. | `postChatFile()` plus `sendChat()` -> `generationChat.ts:2046`. | Pass | Paste/menu path calls `postChatFile()` and `sendChatMain()` sends the composed `{{inlayed::...}}` content. |
| `src/lib/SideBars/CharConfig.svelte:612`, `:763`, `:1139` | avatar/additional asset/reference audio buttons | Character asset upload buttons. | Uploads assets, then patches character fields. | Assets route plus `PATCH /api/v1/commands/characters/:characterId`. | Pass | `selectCharImg()` dispatches character update; draft watcher sends `updateCharacterCommand()`. |
| `src/lib/Setting/Pages/OtherBotSettings.svelte:587`, `:663`, `:954` | media settings asset buttons | Image/reference asset upload controls. | Uploads assets, then patches media settings. | Assets route plus `PATCH /api/v1/commands/settings/:group`. | Pass | `createServerBackedSettingDraft()` queues settings patches through `patchServerBackedSettings()`. |
| `src/lib/Setting/Pages/Module/ModuleMenu.svelte:437` | module asset upload button | Module asset controls. | Uploads assets; module row persists when parent Save is clicked. | Assets route plus `POST/PATCH /api/v1/commands/modules`. | Pass | Child mutates `tempModule.assets`; `ModuleSettings.svelte` Save calls `updateGlobalModule()`. |
| `src/lib/UI/Realm/RealmMain.svelte:227` | Realm import/chat button | Realm character import. | Imports character/chats/assets/memory-related state. | `importRealmCharacterFromServer()` -> `POST /api/v1/import/realm-character`. | Pass | `downloadRisuHub()` first uses server Realm import; server commits with `appendRealmCharacter()`. |
| `src/lib/UI/Realm/RealmPopUp.svelte:152` | Realm popup import button | Realm import from popup. | Same Realm import path. | `POST /api/v1/import/realm-character`. | Pass | Popup Chat button calls `downloadRisuHub(openedData.id)`. |
| `src/lib/UI/Realm/RealmPopUp.svelte:102`, `:124` | Realm report/remove buttons | Remote Realm moderation/removal. | No local DB write. | `authenticatedHubFetch()` to hub `/report` and `/remove`. | N/A | Intentionally remote-service only; no Fastify local handler. |
| `src/lib/Setting/Pages/UserSettings.svelte:82` | save server backup button | Creates a server-side backup snapshot. | Writes backup files/snapshot, not normal command rows. | `createServerBackup()` -> `POST /api/v1/backups`; `createBackup()`. | Pass | Route exists and repository creates backup manifest/files. |
| `src/lib/Setting/Pages/UserSettings.svelte:93` | load server backup button | Lists/restores internal backups. | Restores DB/assets and refreshes projection. | `GET /api/v1/backups`; `POST /api/v1/backups/:id/restore`. | Pass | `restoreServerBackup()` calls route then `forceServerProjectionResync()`. |
| `src/lib/Setting/Pages/UserSettings.svelte:106`, `:117` | save local backup / save zip backup buttons | Downloads server-built backup bundle. | Read-only export of current DB/assets. | `GET /api/v1/export/local-backup`; `GET /api/v1/export/bundle`. | N/A | Intentionally export/download only; routes build blobs and do not restore or mutate DB. |
| `src/lib/Setting/Pages/UserSettings.svelte:130` | load local backup button | Uploads selected backup bundle/bin and restores it. | Replaces DB and registers bundled assets. | `importServerBundle()` -> `POST /api/v1/import/bundle`. | Pass | Route streams upload, `addAssets()`, then `applyImportedDatabase()` and client resyncs. |
| `src/lib/Setting/Pages/UserSettings.svelte:143` | clean cold storage button | Cleans cold-storage data. | No current database change. | `cleanColdStorage()` in `src/ts/process/coldstorage.svelte`. | Issue | `cleanColdStorage()` returns immediately; storage helpers are stubbed and no Fastify route exists. |
| `src/ts/server/backups.ts:57`, `:103`, `:154`, `:250` | backup helpers | Client helpers behind backup UI. | Creates/restores/deletes/imports backup files and restore revisions. | `/api/v1/backups`, `/api/v1/import/bundle`, export routes. | Pass | Helper endpoints exist; restore/import paths call projection resync. |
| `src/ts/server/realmImport.ts:31` | Realm import helper | Client helper behind Realm buttons. | Starts/finishes server Realm import. | `POST /api/v1/import/realm-character`. | Pass | Sends `baseRevision` and active-writer header; parses JSON/SSE success and updates cached revision. |
| `src/ts/characters.ts` import helpers | `importChat`, character import paths | Import buttons in chat/character lists. | Supported JSON/JSONL/HTML chat and character imports persist; `.txt` chat import does not. | Chat/character command routes; assets route; save import routes for bundles. | Issue | `importChat()` picker allows `txt` but has no `.txt` branch; other branches use `dispatchCreateImportedChats()`/`dispatchCreateCharacter()`. |
| `src/lib/Others/HypaV3Modal/server-memory-jobs.svelte:191` | cancel server memory job button | Cancel job button. | Marks a memory job cancelled. | `cancelServerMemoryJob()` -> `DELETE /api/v1/memory/jobs/:id`; `cancelMemoryJob()`. | Pass | Button calls client helper; server transitions pending/running jobs to `cancelled`. |
| `src/ts/process/request/serverMemory.ts` | memory job request helpers | Memory modal or generation-triggered job creation. | Referenced helper does not enqueue jobs. | `POST /api/v1/memory/jobs` exists, but this file only lists/reads/cancels. | Issue | Stale row: no client create helper in `serverMemory.ts`; server-side enqueue happens in planners/followups instead. |
| `src/lib/Others/HypaV3Modal.svelte:261`, `:352`, `:382`, `:393` | local Hypa bulk buttons | Apply bulk resummary, reset data, bulk category, bulk important. | No live server-backed write. | Local-only guarded by `serverBackedMemoryMode`. | N/A | `canUseServerMemoryApi()` is true; handlers return early or UI is hidden in Fastify mode. |
| `src/lib/Others/HypaV3Modal/category-manager-modal.svelte:121`, `:156`, `:161`, `:177` | category add/edit/delete buttons and input | Category manager controls. | No live server-backed write. | Parent renders modal only when `!serverBackedMemoryMode`. | N/A | `HypaV3Modal.svelte` excludes `CategoryManagerModal` in server-backed mode. |
| `src/lib/Others/HypaV3Modal/tag-manager-modal.svelte:102`, `:108`, `:122`, `:127`, `:140` | tag add/edit/delete inputs/buttons | Tag manager controls. | No live server-backed write. | Parent renders modal only when `!serverBackedMemoryMode`. | N/A | `HypaV3Modal.svelte` excludes `TagManagerModal` in server-backed mode. |
| `src/lib/Others/HypaV3Modal/modal-summary-item.svelte:417`, `:436`, `:444`, `:456`, `:511`, `:520` | summary important/delete/edit/apply fields/buttons | Per-summary controls and textareas. | No live server-backed write. | Parent passes `readOnly={serverBackedMemoryMode}`. | N/A | Buttons are hidden/disabled and textareas are readonly in server-backed mode. |

Commands/tests run by verification agent:

- `sed -n '1,240p' STRUCTURE.md`
- `sed -n '1,260p' docs/user-input-layer/assets-imports-backups-memory.md`
- Focused `rg`/`sed` sweeps across referenced `src/` and `server/fastify/src/` files/routes.
- `pnpm exec vitest run src/ts/globalApi.saveAssets.test.ts src/ts/process/request/tests/serverMemory.test.ts src/ts/server/realmImport.test.ts src/ts/characters.importChat.test.ts src/lib/Setting/Pages/Module/ModuleSettings.svelte.test.ts src/ts/process/coldstorage.test.ts`
- Result: 6 files, 31 tests passed.

