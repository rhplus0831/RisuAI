import { fakeRequestChatData } from '../providerFake'

export const requestChatData = fakeRequestChatData
export const requestChatDataMain = fakeRequestChatData

export type StreamResponseChunk = { [key: string]: string }
