# API Security, Runtime, and Network Boundaries

This area covers the Fastify composition boundary: authentication, single-writer ownership, route policy, bootstrap and resource hydration, configuration and shutdown, agent data sandboxing, static serving, request tracing, Web Push, generic and permissioned egress, and local stream jobs.

Assertions in this area are generally strong: most route tests check exact status/body/headers and upstream calls, while security tests also check that parsing, forwarding, or mutation did **not** occur. The most important weakness is uneven configuration and compatibility coverage despite the broad route surface.

## Authentication, active writer, and route policy

| Test group and included cases                                                                                                                                                                                                                                                                                                                                                                                                                                          | Behavior protected                                                                                                           | Importance                                                                 | Effectiveness and attention                                                                                                                                                                                                                                                |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **First-run and browser-compatible auth** — `server/fastify/__tests__/smoke.test.ts`: `walks the no-password setup-login flow and accepts the resulting assertion`; `issues and accepts server session auth for browsers without WebCrypto`. `auth.test.ts`: `agent dev auth bypass > reports authorized and allows protected routes without password setup`; `fallback session authentication > expires server-issued fallback tokens and removes their stored hash`. | Password setup, ECDSA assertion login, expiry, server-issued fallback sessions, and the deliberate agent-development bypass. | Critical: a regression can lock out users or expose every protected route. | Exact public/protected responses and stored-token cleanup make these realistic. The smoke path overlaps focused auth tests, but cheaply protects the full setup/login boundary. Malformed JWT algorithm/key/payload cases are not a visible matrix.                        |
| **Bounded known-key state** — `auth.test.ts`: `keeps the known-key set bounded by the soft cap and evicts the LRU`; `persists the bounded set to disk on every register`; `trims a pre-existing oversize cache on load`.                                                                                                                                                                                                                                               | Prevents an unbounded persistent accumulator while preserving recently authorized keys.                                      | High availability/security value.                                          | Deterministic 4,096-entry tests assert size, eviction, and disk state. They are implementation-aware but directly catch realistic resource growth.                                                                                                                         |
| **Auth occurs once and before work** — `auth.test.ts`: bulk route `rejects unauthenticated requests and verifies authenticated requests exactly once`; proxy/hub `verifies exactly once when protected`; unauthenticated proxy/hub requests `stop before body parsing or forwarding`.                                                                                                                                                                                  | Avoids duplicated expensive signature checks and prevents oversized/untrusted bodies or egress before authentication.        | Critical security and denial-of-service protection.                        | Strong call-count, response, and “fetch not called” or pre-parse assertions. Some module-reset mocking makes the tests coupled to import structure.                                                                                                                        |
| **Active-writer ownership** — the cases in `activeWriter.test.ts`: ownership/epoch survives restart; most recently bootstrapped session wins; passive bootstrap cannot reclaim; stale writers are rejected for commands, import, asset upload, backups, legacy storage, backup restore/delete, memory job create/cancel, memory planning; observe/public asset exceptions remain open.                                                                               | Stops stale tabs or sessions from overwriting current authoritative state while keeping reads/observers usable.              | Critical data-integrity protection.                                        | Excellent cross-route matrix with restart coverage. It asserts the policy at realistic route boundaries, not only the helper.                                                                                                                                              |
| **Writer-header edge cases** — `routeProtection.test.ts`: missing header after latch is stale; empty/whitespace/over-128-byte headers are rejected; fresh servers accept authenticated mutation before latch; hash-aware resource POST reads are exempt.                                                                                                                                                                                                               | Defines the bootstrap window and rejects ambiguous/spoofed writer identities.                                                | Critical for consistent multi-tab behavior.                                | Strong exact status checks across representative mutations and every hash-aware read family.                                                                                                                                                                               |
| **Table-wide route inventory and auth** — `routeProtection.test.ts`: every live API route has a protocol-manifest decision; every manifest-protected route returns 401 without auth; hash-aware POSTs are classified read-only; documented public routes remain public; durable-generation reattach/cancel require auth; expensive GETs reject accidental HEAD; raw proxy bodies authenticate before parsing.                                                          | Makes a newly registered route fail tests if its auth/writer classification is missing or wrong.                             | One of the suite’s highest-value security groups.                          | The live-app sweep catches omissions automatically. Its parser depends on Fastify `printRoutes()` text formatting; a structural route-registration capture would be less version-coupled. Feature suites, rather than this sweep, prove successful authenticated behavior. |
| **Explicit rate limits** — `routeProtection.test.ts`: login accepts the configured number of bad attempts then returns 429; durable-generation reattach streams remain outside ordinary submission limits.                                                                                                                                                                                                                                                             | Protects login from brute force without throttling long-lived observation paths.                                             | High security and reliability value.                                       | Direct route-level assertions use the production constants. The tests do not cover proxy-derived client IP/trust-proxy combinations.                                                                                                                                       |

## Bootstrap, configuration, lifecycle, and static serving

| Test group and included cases                                                                                                                                                                                                                                                                                                                               | Behavior protected                                                                                               | Importance                                                                  | Effectiveness and attention                                                                                                                                                                                          |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Runtime bootstrap** — `bootstrap.test.ts` cases: fresh and post-password unauthenticated rejection; exact uninitialized metadata; initialized revision without database transfer; large-response gzip with byte-identical body.                                                                                                                     | Keeps bootstrap small, authenticated, lineage/revision aware, and compressible.                                  | Critical to every browser boot and recovery flow.                           | Exact whole-object responses are strong and catch accidental state transfer. Compression is tested with a real Fastify compression plugin.                                                                           |
| **Environment parsing** — `config.test.ts` covers import/Realm limits, request and generation trace modes, safety-snapshot policy, and selected initialization options. | Protects bounded import and trace behavior, explicit opt-in of prompt sidecars, and safe destructive replacement defaults. | High where limits, prompt privacy, and recovery are involved. | Assertions are exact. Port, ordinary body limit, data path, trust proxy, static root, Hub/Realm URL validation, host, and agent bypass parsing still need a complete table-driven contract. |
| **Disposable agent data sandbox** — `agentDataSandbox.test.ts` covers clone/keep/fresh modes, WAL-visible rows, hard-linked asset payloads, exclusion of credentials/backups/traces, replacement of an old sandbox, missing-source behavior, and unsafe path rejection. | Keeps `pnpm dev:agent` isolated from the human database while providing a realistic disposable copy. | Critical to safe agent-driven browser work. | Real files and SQLite WAL state make this strong operational coverage; platform-specific link/filesystem behavior is not exercised across operating systems. |
| **Entrypoint shutdown** — `index.test.ts` cases: SIGTERM and SIGINT call `app.close`/`onClose`; duplicate signals reuse one close; hung-shutdown backstop is unrefed and exits with signal-style code.                                                                                                                                                | Prevents database/file corruption and hung deployments during shutdown.                                          | High operational value.                                                     | Good lifecycle sequencing and idempotency assertions. Process/timer mocks are necessarily implementation-coupled; `index.ts` line coverage is 71.66%.                                                                |
| **Health and static SPA** — `smoke.test.ts`: `reports health`. `static.test.ts` cases: root and fallback documents; nested files; immutable `/assets` caching; gzip byte identity; small API uncompressed; API 404 and non-GET separation; API works with and without `staticRoot`; disabled root returns Fastify 404.                               | Protects deployment boot checks and the built SPA/API routing boundary.                                          | High for packaged/self-hosted deployments.                                  | Real temporary files and Fastify injection provide strong coverage. Screenshot/DOM correctness belongs to browser tests, not these route tests.                                                                      |
| **Non-security browser UUIDs** — `src/ts/nonSecurityUuid.test.ts`: prefer `crypto.randomUUID`; otherwise use `getRandomValues` with RFC 4122 version/variant bits; otherwise retain unique RFC-shaped fallback IDs.                                                                                                                                     | Keeps local row/operation identifiers well shaped and collision-resistant enough on restricted browser surfaces. | High data-identity value, but deliberately not an authentication primitive. | Deterministic stubs assert all fallback tiers and bit layout. No high-volume collision/property test exists, and callers must not reinterpret the helper as cryptographically secure.                                |

## Resource hydration, compatibility, and communication cost

| Test group and included cases                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Behavior protected                                                                                | Importance                                          | Effectiveness and attention                                                                                                                                                                                                                  |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | --------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Hash-aware authoritative resources** — `resourceReads.test.ts` cases: masked settings; allowlisted settings groups; exact hash substitutions; aggregate/targeted collections with masked secrets; changed-item inventories; strict inventory validation/cap; root prompt fallbacks and malformed/duplicate preset pointers; character/chat/lorebook stubs; inventory-order-independent substitution; resident lorebooks; narrow order/selection; full/ranged/bulk messages; full/bulk character lorebooks; masked legacy preset and prompt-template detail.    | Protects the thin-browser read protocol, secret masking, cache correctness, and scoped hydration. | Critical to startup, invalidation, and large saves. | Responses, hashes, shells, masks, and fallback shapes are asserted in detail. This is realistic regression coverage rather than simple status testing.                                                                                       |
| **Legacy storage bridge** — `legacyStorage.test.ts` cases: auth; raw hex-path write/read; mid-write and rename failure preserve old bytes/remove temp; missing read; common path validation; empty write; UTF-8 list; single/multi remove; validate-all-before-delete; idempotent missing remove; SHA-256 crypto success and non-string rejection.                                                                                                                                                                                                               | Preserves compatibility without permitting traversal or partial writes.                           | Medium-high while the bridge remains live.          | Atomic failure tests are especially valuable. It overlaps the browser smoke’s assertion that normal Fastify usage no longer calls `/storage`, which is healthy compatibility-vs-modern-path layering.                                        |
| **`Prefer` parsing** — both `http.test.ts` cases: recognizes `return=minimal` through casing/combined headers/parameters; rejects absent or partial tokens.                                                                                                                                                                                                                                                                                                                                                                                                             | Protects compact command acknowledgements.                                                        | Medium; small helper with broad protocol effect.    | Concise, exact coverage.                                                                                                                                                                                                                     |
| **Payload metrics** — `payloadBudgets.test.ts`: one composite case emits bootstrap, character, and chat-message metrics; verifies character shells omit messages/Hypa; metric bytes equal serialization; bootstrap/character responses are smaller than hydration.                                                                                                                                                                                                                                                                                                      | Detects a loss of message-light projections and verifies observability.                           | High intent, moderate gate strength.                | The assertions are relative, not a true budget: all payloads could grow together and still pass. Add reviewed absolute ceilings for representative fixtures.                                                                                 |
| **Large-corpus server load gates** — `serverLoadCostHarness.test.ts`: SQL classifier positive/negative cases plus hot-path cases covering scoped chat/Hypa and bulk hydration, legacy fallbacks, chat create, append cost, character lorebooks, prompt assembly/editinput, asset lookup/table sharing, memory cleanup/selection, scoped-vs-broad loaders, character/collection reads, server-intent completion, clone narrowing, triggers, Realm append, generation finalization, asset GC, secret masking, bootstrap, SSE replay, and disabled/enabled metric work. | Prevents O(database-size) reads, clones, and serialization on user-visible hot paths.             | Critical performance coverage for large saves.      | These tests are unusually meaningful because they run realistic corpora and count SQL/clone work. They are intentionally coupled to storage functions and instrumentation; an architectural rewrite will require coordinated oracle updates. |

## External egress, streaming, and observability

| Test group and included cases                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Behavior protected                                                                                                        | Importance                                                                                                                                                   | Effectiveness and attention                                                                                                                                                                                                                               |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Permissioned plugin network validation** — `pluginNetwork.test.ts`: IP classification cases for `127.0.0.1`, `10.0.0.1`, metadata/link-local, `::1`, mapped loopback/metadata, ULA/link-local/site-local, and documentation IPv6; accepts public hostname only when every DNS answer is public; rejects direct private/metadata/mapped/first-party targets (including `api.risuai.xyz`); rejects mixed public/private DNS; uses hostname boundaries; pins new target connections; follows a public redirect with fetch-compatible method rewriting and cross-origin credential stripping; blocks metadata redirect pivot; aborts pending DNS; preserves encoded bytes. | Prevents SSRF, DNS rebinding between validation/connect, redirect pivots, and credential leakage from plugins.            | Critical security coverage.                                                                                                                                  | Broad IP-family, DNS, redirect and byte-level assertions are strong. Local mocks avoid external flakiness while exercising real policy. Plugin execution and permission behavior are assessed in [Plugins, Modules, and MCP](plugins-modules-and-mcp.md). |
| **Generic proxy lifecycle and wire policy** — `proxy.test.ts` cases. Disconnect group: complete request/response do not abort; premature close and pre-destroyed input do. Fetch group: timeout normalization/cleanup; framing, hop-by-hop, cookies/auth challenges and `risu-*` stripping; auth/missing URL; plugin-private-target blocking without narrowing generic proxy; PATCH/HEAD/OPTIONS plugin methods; status/body/header forwarding; gzip decompression; GET/POST/PUT/DELETE bodies; header override; default/capped/explicit/invalid deadlines; multi-chunk streaming; generous receive timeout; client-disconnect abort.                             | Protects the legacy generic proxy and stricter plugin route from stale headers, leakage, incorrect aborts, and buffering. | Critical security/compatibility value. Exact upstream requests and local multi-chunk responses are strong. Real close/timer ordering is a modest flake risk. |
| **Hub passthrough** — `hub.test.ts` cases: shared deadline; public GET and authenticated mutations/overrides; Realm query translation; no zstd compression; path/query/body/origin forwarding; exact persisted-token injection and rejection of caller credentials/extras/overrides/query variants/missing token; independent body cap; shared response/request header stripping; timeout and upload cap; complete URL overrides; same-origin/relative redirects; cross-origin/non-HTTP/body-bearing redirect rejection; disconnect abort; 502 connection failure.                                                                                                | Protects public content access while containing privileged Realm removal and authenticated override behavior.             | Critical wherever a stored Realm token is involved.                                                                                                          | Strong secret non-leakage and redirect-boundary assertions. Local TCP timing helpers can be sensitive under heavy load.                                                                                                                                   |
| **Local stream target matrix** — `streamJobs.test.ts` parameter cases. Accepted: loopback/private/link-local/unspecified IPv4, `localhost`, `.local`, loopback/mapped/ULA/link-local IPv6, and HTTPS loopback. Rejected: public IPv4/host, out-of-range private prefixes, public/mapped-public IPv6, FTP/file/malformed/empty; plus userinfo stripping and non-string inputs. Timeout/heartbeat cases cover garbage default, max clamp, fractional floor, heartbeat bounds.                                                                                                                                                                                              | Restricts the local-device/model streaming feature to intended local networks and sane timeouts.                          | High security and reliability value.                                                                                                                         | Parameter coverage is explicit and readable. Unlike plugin egress, local stream jobs do not have a redirect/DNS-rebinding test matrix; add one if redirects are permitted by the implementation.                                                          |
| **Job registry and upstream execution** — `streamJobs.test.ts`: lifecycle cases (activity detection, buffer/flush/broadcast, Agent progress replay compaction, slow-client detach, event/byte caps, detach/delete/GC, sliding/fixed/silent deadlines, stale cleanup); execution cases (headers/body/x-forwarded-for, non-local error, midstream abort, no-viewer overflow abort). `streamBackpressure.test.ts`: buffered-byte/cap/close/write cases.                                                                                                                                                                                                           | Prevents memory growth, stuck jobs, incoherent late replay, and slow-viewer process pressure.                             | High availability value.                                                                                                                                     | Exact pending bytes/events/client state and real upstream closure make regressions likely to be caught. A few tests use real 10–25 ms polling/abort timing.                                                                                               |
| **Stream-job HTTP/WebSocket API** — `streamJobsRoutes.test.ts` cases. POST: pre-setup/agent-bypass/post-password auth, success shape, non-local URL, method, body cap. DELETE: cancel and unknown idempotency. WebSocket: ordered accepted/headers/chunk/done; auth through subprotocol; query credential rejection; unauthenticated/unknown rejection; pre-attach flush; completed-job viewer closes.                                                                                                                                                                                                                                                            | Protects authentication and replay semantics across HTTP-to-WebSocket handoff.                                            | High, particularly the no-query-credential rule.                                                                                                             | Strong ordered frames and real local WebSockets. Two cases use fixed 80 ms waits and one uses a 3-second close guard; prefer observable barriers.                                                                                                         |
| **Request abort helpers** — `requestAbort.test.ts` cases: default 600-second reference; valid request remains alive; deadline abort; refresh extension; max cap; completed/incomplete request and response close distinctions; manual abort; listener/timer cleanup; detached work survives caller close but honors its deadline.                                                                                                                                                                                                                                                                                                                                 | Defines when server work belongs to a request and when it must survive a browser disconnect.                              | Critical for generation/provider correctness and leak prevention.                                                                                            | Fake timers make deadline cases deterministic; event-listener assertions are strong. Route suites add real-close integration.                                                                                                                             |
| **Request tracing** — `requestTrace.test.ts` cases: disabled behavior; non-API UID without JSONL; API JSONL; route/caller/query redaction; referer/user-agent/writer fallback; small-body redaction; large gzip sidecars; compressed cap omission; streaming omission; selected mode; 5,000-row retention and sidecar cleanup.                                                                                                                                                                                                                                                                                                                                    | Preserves useful diagnostics without leaking secrets or unbounded disk use.                                               | High privacy/operations value.                                                                                                                               | Strong content, path, gzip, redaction and cleanup assertions. Exact trace formatting is implementation-coupled; polling loops have a small flake window.                                                                                                  |
| **Web Push** — `pushNotifications.test.ts`: sends bounded completion payloads, resolves chat/character context, skips disabled delivery, prunes gone subscriptions, and protects subscription mutation. `src/ts/server/pushNotificationRetryStorage.test.ts` validates bounded endpoint hydration plus persistent, corruption-safe cleanup retry state. | Protects notifications without unbounded payloads or permanently stranded stale subscriptions. | Medium-high user value. | Good server state, mock-send, and durable client-ledger assertions. It does not validate a real push service/VAPID exchange. |

## API-centered browser coverage

`server/fastify/browser-smoke` runs a built SPA against an in-process Fastify server on a random port. The suite is serial and has no retries.

| Browser case                                                                                                                | Server/API behavior included                                                                                                                                                                                                                                                 | Regression value and attention                                                                                                                                                                                      |
| --------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `fastifyBrowserSmoke.spec.ts > Fastify-served browser loads bootstrap, subscribes to events, and refreshes after a command` | Real bootstrap, SSE subscription, settings/collections/characters hash-aware reads, settings command/event, refreshed projection, echo completion, memory reads, `.risu` export/import, bundle export, asset upload/read, no legacy `/storage` requests, and no OPFS writes. | The highest-value runtime wiring smoke. It asserts real request paths, hashes, statuses, event shape and visible refreshed state. It is deliberately broad, so component tests provide better failure localization. |
| `phase0VisibleState.spec.ts > Journey 2 (settle): a sidebar toggle flip survives the command + resource refresh`            | A real generation-settings command followed by SSE/resource refresh; the DOM and stored projection remain off.                                                                                                                                                               | Strong visible-state proof synchronized to the exact command response, applied resource revision, and settled DOM/store state.                                                                                     |
| `phase0VisibleState.spec.ts > Journey 3 (GATE): the same-character sidebar view survives old-lineage recovery after import` | A held pre-import command resumes after lineage rotation, receives an exact conflict, and drives a real recovery reload while route and selected character remain valid.                                                                                                       | Valuable import/resync wiring synchronized to the conflict response, navigation, new document, applied import revision, and visible state. The primary UI-state assessment is in [Browser State Sync and Recovery](browser-state-sync-and-recovery.md).                             |
| `phase0VisibleState.spec.ts > Journey 1: switching chats repaints the active-chat generation picker`                        | Initial authoritative resource hydration; the transition itself is client routing/rendering.                                                                                                                                                                                 | Secondary backend relevance; assessed primarily in [Browser State Sync and Recovery](browser-state-sync-and-recovery.md).                                                                                           |
| `rerollSwipePersistence.spec.ts > rerolled candidates survive a reload and stay swipe-recoverable`                          | Server generation and alternate persistence are central.                                                                                                                                                                                                                     | Assessed in [Prompting, Generation, and Streaming](prompting-generation-and-streaming.md); cross-reference here only as evidence that runtime bootstrap/hydration reconnects to persisted state.                    |
| `fastifyBrowserSmoke.spec.ts > core chat controls and blocking alerts remain accessible across responsive viewports`        | Static serving and initial boot only; owns four screenshots.                                                                                                                                                                                                                 | Primarily frontend accessibility/visual coverage, not part of this backend assessment.                                                                                                                              |

## Coverage gaps and recommendations

- Add direct tests for all core `loadConfig` parsers, especially `TRUST_PROXY`, Hub/Realm URLs, static-root disablement, port/body limits, and agent bypass.
- Replace the relative payload assertion with explicit reviewed byte ceilings; retain the shell-vs-hydration semantic assertions.
- Make route enumeration structural rather than parsing `printRoutes()` text if Fastify offers a stable hook/registry.
- Add redirect and resolved-address behavior for local stream jobs, matching the rigor of plugin egress, if the feature intentionally follows redirects.
- Replace fixed millisecond sleeps and tiny real-time aborts with server/test barriers or condition polling; keep a small number of real deadline integration cases.
- Publish backend coverage in CI or add narrow thresholds for security-critical modules. The ordinary server lane has no coverage gate.

## Complete primary file manifest

| Protection area          | Primary test files                                                                                                                                     |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Auth and route policy    | `activeWriter.test.ts`; `auth.test.ts`; `routeProtection.test.ts`; `smoke.test.ts`                                                                    |
| Startup and deployment   | `agentDataSandbox.test.ts`; `bootstrap.test.ts`; `config.test.ts`; `index.test.ts`; `static.test.ts`                                                   |
| Resources and compatibility | `http.test.ts`; `legacyStorage.test.ts`; `payloadBudgets.test.ts`; `resourceReads.test.ts`; `serverLoadCostHarness.test.ts`                         |
| Egress and Hub           | `hub.test.ts`; `pluginNetwork.test.ts`; `proxy.test.ts`; `requestAbort.test.ts`                                                                         |
| Streaming and tracing    | `requestTrace.test.ts`; `streamBackpressure.test.ts`; `streamJobs.test.ts`; `streamJobsRoutes.test.ts`                                                  |
| Web Push                 | `pushNotifications.test.ts`                                                                                                                             |
| Browser counterparts     | `src/ts/nonSecurityUuid.test.ts`; `src/ts/server/pushNotificationRetryStorage.test.ts`; `src/ts/sha256Fallback.test.ts`                                 |
