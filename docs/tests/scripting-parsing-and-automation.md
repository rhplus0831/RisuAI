# Scripting, Parsing, and Automation

This area covers CBS/template parsing, ChatML and rendered-message parsing, regex scripts, slash commands, trigger effects, Lua/Python execution, resource-guarded automation writes, and script-definition editing. Prompt assembly uses many of these features and is cross-referenced in [Prompting, Generation, and Streaming](prompting-generation-and-streaming.md).

## Assessment

The parser tests are compact and valuable: most feed concrete source text to production parsers and assert exact output, including nested control flow, whitespace, escaping, recursion, and budget failures. Trigger/script tests are strongest where they prove durable writes under the resource guard, bounded execution, scoped rollback, cache behavior, and server-side Lua security. The area is less complete than its case count suggests. Large legacy trigger/CBS engines have many branches, client and server implementations lack a comprehensive parity table, and editor/UI tests do not exercise every runtime effect they can author.

## Test groups

| Logical group                                      | Relevant locations and included cases                                                                                                                                                                                                                              | Behavior and importance                                                                                                                                                                                                                                                                                                                                                         | Effectiveness and overall value                                                                                                                                                                                                                                                                                                                                                               |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CBS conditionals, loops, strings, and escapes      | `src/ts/parser/tests/cbs/{conditionals,eachReinjection,escapes,history,loop,strings}.test.ts` (77 cases total)                                                                                                                                                     | Cases cover `#if`, `#if_pure`, `#when`, comparisons/logical operators/right-to-left evaluation, variable/toggle operators, else/nesting, `#each` literals/variables/nesting/reinjection and element/output caps, history copies, escape tokens, `#pure`, string transforms, default variables, Unicode, matcher aliases, and recursion limits.                                  | **Strong pure regression coverage.** Exact strings and property-based escape inputs protect realistic prompt/template behavior. It also exposes weak tests: the case titled `:` invokes `{{;}}` and therefore repeats semicolon coverage; `can omit "as"` contains the same assertion twice; two distinct 2-D loop cases share one title. These should be fixed/renamed for diagnostic value. |
| Chat/message parsing and partial edits             | `src/ts/parser/partialEdit.test.ts`; `tests/{chatML,chatVar.svelte,inlayBlobCache,renderFastPaths}.test.ts`; Chat/ChatBody parser-dependency and custom-HTML UI tests                                                                                              | Verifies normalized partial-edit mapping and fallback cap; ChatML roles/thoughts/empty segments; character/template/global chat variables; cached asset reads, LRU/revocation and hide-images; nested thoughts/tool calls; and parser fast paths/no logging.                                                                                                                    | **Good output and resource-lifecycle coverage.** Partial-edit has only two direct cases, and broader markdown/custom-HTML behavior relies heavily on component mocks. Real browser sanitization and selection/caret behavior remain thin.                                                                                                                                                     |
| Regex scripts and display transformations          | `src/ts/process/{scripts.editdisplay,scripts.importRegex,scripts.regexCache}.test.ts`, server `scripts.test.ts` (49), and SideBars script editor tests                                                                                                             | Covers edit-display injection, message patch/rollback, active-preset ownership, malformed rows, import cancellation/validation/legacy normalization, compiled-regex and best-match LRU caches, directives, moves/repeats, DSL actions, output preparation, and prepared-script memoization.                                                                                     | **High value.** Exact transformed text/state and no-write/no-log assertions catch user-visible regressions. Cache instance/compile-count checks are intentionally implementation-coupled. Import/editor tests do not prove that every authored combination executes correctly after persistence/reload.                                                                                       |
| Slash commands and guarded durable effects         | `src/ts/process/__tests__/command.resourceGuard.test.ts` (19) and chat/file command tests                                                                                                                                                                          | Covers `/send`, `/sendas`, `/comment`, `/cut` by range/index/id, `/del`, `/multisend`, `/setvar`, `/addvar`, pipe returns, active-chat changes, scoped failure rollback, absence of whole-database writes/clones, and warm-path logging.                                                                                                                                        | **Critical.** These cases assert command payloads and resulting transcript/scriptstate while the direct resource write guard is enabled. They are stronger than tests that merely spy on command invocation. Visible DOM results and browser reload are not covered here.                                                                                                                     |
| Lorebook resource guards and stable import targets | `src/ts/process/__tests__/lorebook.resourceGuard.test.ts` (9)                                                                                                                                                                                                      | Refuses to mutate, import, or export unhydrated lorebook stubs; dispatches global add/folder/import operations through commands; captures the original character/chat/global owner across asynchronous file selection; and rolls back only attempted fields while preserving concurrent edits.                                                                                  | **Critical data-loss coverage.** Exact no-write, target, payload, and rollback assertions protect a particularly dangerous stale-stub path. File picking/transport and the rendered lorebook editor remain mocked, so one mounted/import-reload journey is still needed.                                                                                                                      |
| Trigger routing, control flow, and budgets         | Client `triggers.{clientBudget,cloneCost,regexMemo}.test.ts` plus `__tests__/triggers.resourceGuard.test.ts`; server `triggers.test.ts` (86)                                                                                                                       | Includes source/module matching, variables and conditions, V1/V2 effects, loop/wait/abort/resend budgets, cache and bounded regex, data operations, display/request state, character/persona/lorebook/author-note/scriptstate durable writes, stale targets, and narrow rollback.                                                                                               | **Critical but uneven.** Control-flow, budget, durable-write, and representative data effects are strong. Backend V8 coverage shows `prompt/triggerDataEffects.ts` at only 41.72% lines; many string, array, dictionary, random, and message-reader arms have no direct cases. Clone/compile counters overlap behavioral cases but guard performance.                                         |
| Lua runtime and host authority                     | `src/ts/process/scriptings.test.ts` (20), server `luaRuntime.test.ts` (36), and Lua cases in generation/trigger tests                                                                                                                                              | Covers client edit-display fallback, time-bounded infinite loops, engine/code-hash LRU reuse, current-run host bindings, access-key cleanup, scoped Fastify chat/scriptstate writes, stale targets, description type validation, server network SSRF/rate/abort restrictions, low-level LLM/lorebook APIs, aggregate execution limits, engine pooling, and interactive failure. | **Security-critical and good.** Server cases run the Lua engine and assert network/state effects; client cache tests validate cleanup and current-run binding. Lua remains only 76.42% line, 60.62% branch, and 60.74% function covered on the backend, leaving host and failure paths worth targeting.                                                                                       |
| Python worker protocol                             | Python group in `src/ts/process/scriptings.test.ts`                                                                                                                                                                                                                | Four cases cover init plus a structured call, synchronous host APIs during a request, host failure propagation, and rejection when a cached context terminates.                                                                                                                                                                                                                 | **Useful but narrow.** The protocol basics are asserted, but there is no real Pyodide/browser-worker execution, cancellation/timeout matrix, large transfer, concurrent contexts, or cleanup/restart journey.                                                                                                                                                                                 |
| Definition bridges and editing UI                  | `src/ts/server/{scriptDefinitionBridge.svelte,scriptDefinitionMutations}.test.ts`; `src/lib/SideBars/Scripts/{DefinitionDeleteRace,RegexData,RegexList,TriggerV1Data,TriggerV2List}.svelte.test.ts`; `triggerV2Import.test.ts`; trigger language-description tests | Covers compact create/update/delete/reorder classification, dirty draft merges, character/module ownership, watcher rollback/flush, delete races, import validation, editor controls, and localized descriptions.                                                                                                                                                               | **Strong state, mixed UI depth.** Bridge suites assert narrow rollback and concurrent edits well. Some component cases focus on callbacks/element existence and do not run the saved definition through the production engine.                                                                                                                                                                |

## Especially critical tests

- CBS nested/recursive/budget cases prevent malformed or hostile templates from hanging generation.
- Resource-guard command and trigger suites protect the Fastify-only architecture from accidental direct database writes and broad rollback.
- Server Lua SSRF, execution-budget, abort, and low-level-call cases protect a powerful extension boundary.
- Trigger client/server budget and regex-cap tests prevent runaway automation on chat send or render.
- Definition delete-race and stale-owner bridge cases prevent one editor from deleting or restoring a newer definition.

## Attention and gaps

- Correct the false colon assertion in `cbs/escapes.test.ts`, remove the duplicated omit-`as` assertion, and give the literal/variable 2-D loop cases distinct titles.
- Build a shared CBS compatibility matrix for browser and server adapters: supported value, rejected host-only callback, fallback value, mutation behavior, and error semantics. Backend `prompt/cbsAdapter.ts` is only 30.76% line-covered.
- Add table-driven coverage for the missing V2 trigger data operations rather than another broad happy-path trigger fixture.
- Run a saved regex/trigger/Lua definition from its real editor through persistence, reload, and generation/display in one browser journey.
- Expand Python worker tests to real worker startup/termination, abort/deadline, multiple concurrent calls, large/error results, and context replacement.
- Separate behavior assertions from explicit performance gates where the same test currently asserts both exact output and clone/cache internals.

## Primary inventory

| Location                  | Included test files / groups                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Parsers                   | `src/ts/parser/partialEdit.test.ts` (2); `src/ts/parser/tests/chatML.test.ts` (5), `chatVar.svelte.test.ts` (6), `inlayBlobCache.test.ts` (5), `renderFastPaths.test.ts` (7); CBS `conditionals` (27), `eachReinjection` (10), `escapes` (12), `history` (3), `loop` (11), `strings` (14).                                                                                                   |
| Client automation runtime | `src/ts/process/scriptings.test.ts` (20), `scripts.editdisplay.test.ts` (10), `scripts.importRegex.test.ts` (5), `scripts.regexCache.test.ts` (7), `triggers.clientBudget.test.ts` (4), `triggers.cloneCost.test.ts` (2), `triggers.regexMemo.test.ts` (6), `__tests__/command.resourceGuard.test.ts` (19), `lorebook.resourceGuard.test.ts` (9), and `triggers.resourceGuard.test.ts` (13). |
| Server runtime            | `server/fastify/__tests__/boundedRegex.test.ts` (7), `luaRuntime.test.ts` (36), `promptVariables.test.ts` (19), `scripts.test.ts` (49), and `triggers.test.ts` (86); scripting groups inside `assemble.test.ts` and `generation.chat.test.ts`.                                                                                                                                               |
| Durable definition state  | `src/ts/server/scriptDefinitionBridge.svelte.test.ts` (71), `scriptDefinitionMutations.test.ts` (14), and definition command groups in browser/server command suites. These are primarily inventoried in [Domain Mutations and Editing Bridges](domain-mutations-and-editing-bridges.md).                                                                                                    |
| UI                        | `src/lib/SideBars/Scripts/DefinitionDeleteRace.svelte.test.ts`, `RegexData.svelte.test.ts`, `RegexList.svelte.test.ts`, `TriggerV1Data.svelte.test.ts`, `TriggerV2List.svelte.test.ts`, `triggerV2Import.test.ts`, plus language trigger-description tests.                                                                                                                                  |
