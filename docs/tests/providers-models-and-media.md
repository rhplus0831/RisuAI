# Providers, Models, and Media

This area covers durable model profiles and role bindings, provider capability/routing, provider-specific wire adapters, secret ownership, fixed server operations, translation, image generation, transcription, TTS, and browser media processing. Prompt orchestration is assessed in [Prompting, Generation, and Streaming](prompting-generation-and-streaming.md); MCP OAuth is assessed in [Plugins, Modules, and MCP](plugins-modules-and-mcp.md).

## Assessment

The provider suite has excellent contract-level breadth. Tests assert selected model/profile precedence, URL, headers, credentials, body fields, multimodal conversion, streaming frames, finish/error metadata, aborts, byte limits, and secret redaction for every supported provider family. The model-profile tests are similarly thorough across durable and legacy resolution. This makes accidental credential exposure or a wrong wire shape unlikely to pass unnoticed.

The unavoidable weakness is upstream realism. Normal tests replace all commercial services with mocked fetches or local echo servers. They can prove what RisuAI sends and how it parses recorded shapes, but not that an upstream API still accepts the request today. Browser audio/canvas/image/worker behavior is also largely mocked. There is no opt-in recorded/live provider canary lane and no non-Chromium media journey.

## Test groups

| Logical group                                                 | Relevant locations and included cases                                                                                                                                                                                                                                                                                                                       | Behavior and importance                                                                                                                                                                                                                                                                                                                                                                                                | Effectiveness and overall value                                                                                                                                                                                                                                                                                                                   |
| ------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Model-profile and split-preset records, roles, and resolution | `src/ts/model/{modelProfileRecords,modelProfileResolver,modelProfileUiState,modelRoles,modelPresetSnapshots}.test.ts` (75 cases total), `modelProfileMutations.test.ts`, `src/ts/presetSplit.test.ts` (12), `presetFieldMirror.test.ts` (3), server `modelProfileResolver.server.test.ts`, model-profile command groups, and Settings Model component tests | Covers normalization/schema, durable/legacy/inherit role bindings, profile/runtime/default precedence, fallbacks, static models, provider-specific option resolution, incomplete/unsupported status, legacy UI visibility and mutations; split model/prompt extraction, dedupe, role ownership, export/composition/aliases/cloning; and delayed ID-bound top-level mirroring without generic `promptTemplate` leakage. | **Critical and strong.** The 47-case browser resolver matrix and exact split composition catch realistic wrong-model/prompt routing. Server import-topology coverage is architectural rather than behavioral; cross-layer create/bind/reload/dispatch is missing. UI cases sometimes test helpers/child props more than the fully mounted editor. |
| Dynamic catalog credential isolation                          | `src/ts/model/modellist.dynamic.test.ts` (1)                                                                                                                                                                                                                                                                                                                | Registers dynamic Google and Anthropic models while ensuring catalog credentials are not exposed to a cross-origin consumer.                                                                                                                                                                                                                                                                                           | **Narrow but security-relevant.** The negative credential oracle is exact. One case does not cover plugin/custom registration, duplicate/unregister behavior, catalog refresh, or every provider; broaden the table if dynamic registration expands.                                                                                              |
| Secret masking and stable identity                            | `modelProfileSecrets.test.ts`, `providerSecretMask.test.ts`, browser fixed-operation adapters, server `providerSecrets.test.ts` (9), `chatDispatchProfileOptions.test.ts` (47), resource-read masking, and route tests                                                                                                                                      | Covers preserve/replace/clear draft semantics, nested registered secrets, stable profile/row restoration, unknown/moved identity rejection, copy/in-place parity, stored-sentinel handling, endpoint-key binding, and sanitized errors/traces.                                                                                                                                                                         | **Security-critical.** Assertions check both outbound requests and absence of raw keys in projections/logs. A full browser-to-Fastify profile save/reload/use journey would better prove the boundary across serialization and masking.                                                                                                           |
| Capability and role routing                                   | Client `request/tests/{providerCapability,modelRoleRouting,serverPromptAssembly}.test.ts`; server `providerCapabilityRoute.test.ts`, generation routes, and profile option matrices                                                                                                                                                                         | Expanded cases classify OpenAI, OpenRouter, NanoGPT, Anthropic, Gemini/Vertex, Bedrock, Mistral, Cohere, Horde, Kobold, Ooba, Ollama, custom/reverse-proxy/key-identifier and browser-only/incomplete variants; role cases cover primary/fallback/static/script modes, banned responses, tool-only success, and parameter inheritance.                                                                                 | **Critical.** It prevents selecting a browser-visible model that Fastify cannot execute. Client/server tables overlap but are hand-maintained, so a shared capability fixture should supplement the independent assertions.                                                                                                                       |
| OpenAI-compatible families                                    | Server `openai.test.ts` (38), `openaiResponses.test.ts` (12), `openaiLegacyInstruct.test.ts` (13), `generation.completion.test.ts`, `additionalParams.test.ts`, `providerMessages.test.ts`; client `openaiProfileOptions.test.ts` (12), `openaiResponsesLegacyProfileOptions.test.ts` (5), and additional-param tests                                       | Covers Chat/Responses/legacy completion bodies, system/Ooba hoisting, OpenRouter/NanoGPT/custom/reverse-proxy/Ollama-cloud variants, hosted search/tools, JSON controls, additional headers/body/path parameters and prototype safety, multimodal/reasoning rows, SSE fragmentation, metrics, errors, and abort.                                                                                                       | **Strong wire coverage.** Exact request assertions catch realistic regressions, and prototype/extra-header controls add security value. Upstream schema/model evolution and tool-round interoperability are mocked.                                                                                                                               |
| Anthropic, Bedrock, Gemini, and Vertex                        | Server `anthropic.test.ts` (24), `bedrock.test.ts` (18), `gemini.test.ts` (41), `sigv4.test.ts` (8), `vertexAuth.test.ts` (12); client `anthropicProfileOptions.test.ts` (5) and `google.fastify.test.ts` (4)                                                                                                                                               | Covers native message/media/thinking/tool conversion, cache points, buffered/streaming UTF-8, Bedrock credential parsing and deterministic signing, Gemini thoughts/tools/metrics, Google AI Studio vs Vertex selection, JWT exchange/cache/deduplication/expiry, and abort/error bounds.                                                                                                                              | **High value.** Deterministic signing vectors and credential precedence are strong. No official recorded-response corpus guards less common content blocks or upstream error variants.                                                                                                                                                            |
| Other text providers                                          | Server `cohere` (19), `mistral` (33), `ollama` (30), `horde` (17), `kobold` (7), `oobaLegacy` (6), and `echo` (8); client corresponding profile-option tests and `src/ts/horde/getModels.test.ts`                                                                                                                                                           | Covers message reformats, endpoint/model/key/sampler precedence, safety modes, native and OpenAI-compatible Ollama tools, NDJSON/SSE/WebSocket/poll lifecycles, model discovery caching, anonymous Horde, malformed frames, abort, and bounds.                                                                                                                                                                         | **Good broad compatibility coverage.** Echo is cheap layered smoke rather than redundant provider depth. Legacy/browser transports are more implementation-coupled and may have lower production importance, but prevent imported-config regressions. No real local Ollama/Ooba/Kobold interop lane exists.                                       |
| Fixed provider and embedding operations                       | Browser `src/ts/server/{providerOperations,embeddingOperations}.test.ts`; server `providerOperations.test.ts` (29), `embeddingOperations.test.ts` (13); NanoGPT/OpenRouter/Ollama catalogs and dashboard tests                                                                                                                                              | Tests the allowlisted operation discriminator, typed input validation, stored/provided credential selection, endpoint binding, response/deadline/byte/vector bounds, disconnect cancellation, error sanitization, and keyed cache/dedupe/retry behavior.                                                                                                                                                               | **Security-critical and strong.** These tests prove raw stored credentials need not enter browser provider code. Cache TTL/eviction and server-side credential rotation are not covered for every catalog; most client cache cases use controlled clocks/requests.                                                                                |
| Translation and raw message translation                       | `src/ts/translator/{bergamotTranslator,presets,translator.cache,translator.html}.test.ts` (37), server `rawMessageTranslation.test.ts` (15), provider-operation translation cases, translator UI/Playground tests                                                                                                                                           | Covers worker queue recovery, preset normalization and encrypted codec, role/profile keyed caches, secret-free signatures, quota fallback, HTML memo/edit regexes, streaming guards, DeepLX mismatch fanout/failure, raw message provider resolution, multimodal preservation, async persistence, abort, and sanitized errors.                                                                                         | **High behavior value.** Cache keys and stale-state cases are thorough. HTML/worker/service behavior is mocked, and no real translation result is checked across provider API + rendered DOM.                                                                                                                                                     |
| Image generation and transcription                            | Browser `src/ts/process/{stableDiff}.test.ts`, `__tests__/{igp,imggenStableDiff}.test.ts`, `src/ts/server/{imageGeneration,openAITranscription}.test.ts`; server `imageGeneration.test.ts` (19), `openAITranscription.test.ts` (5); image/translation Playground cases                                                                                      | Covers NovelAI, DALL-E, Stability, Fal, Imagen, WaveSpeed, Kei and OpenAI-compatible shapes; reference asset fallback, stored secrets, target supersession, cancellation, response types/caps/errors, prompt routing, and fixed Whisper upload/VTT validation.                                                                                                                                                         | **Strong request and stale-target coverage.** Actual generated image decoding/rendering and real media uploads are absent. Provider variation is represented through mocks and may drift.                                                                                                                                                         |
| TTS and browser audio lifecycle                               | `src/ts/process/tts.test.ts` (22), `ttsHooks.test.ts` (10), `transformers.test.ts` (5), browser `src/ts/server/tts.test.ts` (4), server `tts.test.ts` (17), TTS asset/UI tests                                                                                                                                                                              | Covers Web Speech absence/injection, voice-catalog cache/retry, masked and draft credentials, every server-owned provider path, PCM-to-WAV, AudioContext/source/gain reuse and cleanup, stop/supersession/late transport, hook sequencing/skip/error/timeout/abort, Hugging Face translation, VITS model disposal and abort.                                                                                           | **High reliability coverage with mocked APIs.** Cancellation and no-secret/no-log assertions are strong. It does not prove browser codec support, audible output, real Web Speech voices, or mobile autoplay behavior.                                                                                                                            |
| Image codecs and file media helpers                           | `src/ts/media/tests/imageType.test.ts` (9), `compressImage/tests/{compressImage,lossyCompression}.test.ts` (11), `src/ts/process/dynamicutils/pdf.test.ts` (4 cleanup/failure cases), plus inlay/CharX tests                                                                                                                                                | Covers format magic bytes, compression bypass/dispatch, browser decode/canvas failure settlement, quality value, image bounds/aspect ratio, object-URL cleanup, awaited PDF document/page/loading-task cleanup on success and each failure point, and oversized archive/media rejection.                                                                                                                               | **Good safety/unit coverage, limited fidelity.** Resource cleanup assertions are strong. Canvas, decoders, and PDF.js are mocked, so color/orientation/animation/transparency and real compression/PDF fidelity are not verified.                                                                                                                 |

## Especially critical tests

- Secret masking/restoration and fixed-operation endpoint binding prevent credential disclosure and confused-deputy requests.
- Capability/routing parity prevents settings from advertising configurations that Fastify cannot execute.
- Provider stream fragmentation, terminal error, byte-cap, and abort cases protect long-running chat reliability.
- SigV4/Vertex token cache and credential-isolation tests protect expensive and security-sensitive authentication flows.
- TTS/image supersession and cancellation tests prevent stale media from applying to a new character/chat target.

## Attention and gaps

- Add an opt-in canary lane using sanitized recorded responses and, where credentials are available, tightly bounded live requests. Version fixtures by upstream API date/model family.
- Add one cross-layer profile journey: create and bind in the browser, reload masked resources, resolve the role, generate through Fastify, and prove the key never appears in browser state or traces.
- Generate capability/profile option parity from shared typed fixtures while retaining provider-owned request assertions.
- Add real Chromium media tests for representative PNG/JPEG orientation/transparency, audio decode/stop/autoplay, PCM wrapping, and image object-URL cleanup; add Firefox/WebKit where feasible.
- Target low backend seams rather than only the global 86.04% line figure: `generation/serverTools.ts` is 73.13% lines, `prompt/chatDispatch.ts` 73.93%, and several provider branches remain in the 60s/70s.
- Expand cache expiration/eviction and credential-rotation cases for all dynamic provider catalogs.

## Primary inventory

| Location                                | Included test files / groups                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Browser model records/catalogs          | All 11 tests under `src/ts/model/`: `modelPresetSnapshots`, `modelProfileMutations`, `modelProfileRecords`, `modelProfileResolver`, `modelProfileSecrets`, `modelProfileUiState`, `modelRoles`, `modellist.dynamic`, `nanogpt`, `ollama`, and `openrouter`; plus `providerSecretMask.test.ts`.                                                                                                                                                                                                                                                                                                                      |
| Split preset contracts                  | `src/ts/presetSplit.test.ts` (12 extraction/composition/ownership cases) and `src/ts/presetFieldMirror.test.ts` (3 stable-field-mirror cases). Mutation/replay behavior is cross-indexed in [Domain Mutations and Editing Bridges](domain-mutations-and-editing-bridges.md).                                                                                                                                                                                                                                                                                                                                        |
| Browser request adapters                | `src/ts/process/request/tests/additionalParams.test.ts` (4), `anthropicProfileOptions` (5), `cohereHordeOobaLegacyProfileOptions` (10), `google.fastify` (4), `koboldProfileOptions` (3), `modelRoleRouting` (13), `ollamaProfileOptions` (9), `openaiProfileOptions` (12), `openaiResponsesLegacyProfileOptions` (5), and provider-capability/completion cases cross-indexed with prompt generation.                                                                                                                                                                                                               |
| Catalog, translation, and media runtime | `src/ts/horde/getModels.test.ts` (3); all four `src/ts/translator/*.test.ts` files (37); all three `src/ts/media/**/*.test.ts` files (20); `src/ts/process/dynamicutils/pdf.test.ts` (4), `stableDiff.test.ts` (13), `transformers.test.ts` (5), `tts.test.ts` (22), and `ttsHooks.test.ts` (10).                                                                                                                                                                                                                                                                                                                   |
| Browser fixed operations                | `src/ts/server/providerOperations.test.ts` (5), `embeddingOperations.test.ts` (4), `imageGeneration.test.ts` (4), `openAITranscription.test.ts` (3), `tts.test.ts` (4), `nanoGPTDashboardFetch.test.ts` (6), and settings-media upload tests.                                                                                                                                                                                                                                                                                                                                                                       |
| Server providers/media                  | The 29-file / 588-case backend provider inventory: `additionalParams`, `anthropic`, `bedrock`, `chatDispatchProfileOptions`, `cohere`, `echo`, `embeddingOperations`, `gemini`, `generation.completion`, `horde`, `imageGeneration`, `kobold`, `mcpOAuthRefresh`, `mistral`, `modelProfileResolver.server`, `ollama`, `oobaLegacy`, `openAITranscription`, `openai`, `openaiLegacyInstruct`, `openaiResponses`, `providerCapabilityRoute`, `providerMessages`, `providerOperations`, `providerSecrets`, `providerTransport`, `sigv4`, `tts`, and `vertexAuth`. MCP OAuth is analyzed primarily in the MCP document. |
| UI                                      | Model settings/list/editor/secret/runtime/fallback tests under `src/lib/Setting/Pages/Model/`; provider list/actions, NanoGPT/OpenRouter model UI tests; Playground translation/image/tool tests. Detailed interaction/accessibility quality is assessed in the UI documents.                                                                                                                                                                                                                                                                                                                                       |
