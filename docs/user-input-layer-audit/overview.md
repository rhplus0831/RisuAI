# User Input Persistence Overview Audit

Audit method: code-level verification of the common client helpers and Fastify route families referenced by `docs/user-input-layer/overview.md`. No browser smoke test was run for this overview file.

## Common Write Paths

| Source | Unique id | Control | Database change | Server handling | Verify | Reason |
| --- | --- | --- | --- | --- | --- | --- |
| `src/ts/server/commands.ts:2830`, `:2875` | `runServerCommand`, `requestCommandJson` | Shared command transport. | Sends revisioned commands under `/api/v1/commands/*`. | `server/fastify/src/routes/commands.ts` command routes. | Pass | `runServerCommand` obtains `baseRevision`, calls the command factory, rolls back on error, and `requestCommandJson` sends auth/active-writer headers to `COMMAND_ENDPOINT`; `registerCommandRoutes` defines the command routes and each mutation uses `applyTargetedCommandMutation`. |
| `src/ts/server/commands.ts:1203` | `patchSettingsGroup` | Settings group patch helper. | Writes settings groups. | `PATCH /api/v1/commands/settings/:group`. | Pass | `patchSettingsGroup` sends `{ baseRevision, patch }` to `/settings/:group`; the server route validates the group and patch, applies `applySettingsPatch`, writes settings rows, and emits a command event. |
| `src/ts/server/settingsBridge.svelte.ts:45`, `:49`, `:107` | `applyServerBackedSetting`, `createServerBackedSettingDraft`, `applyServerBackedSettingsPatch` | Server-backed settings draft/patch helpers. | Debounced or immediate settings patch. | `PATCH /api/v1/commands/settings/:group`. | Pass | Draft effects and immediate patch helpers filter keys through `settingsGroupForKey`, optimistically update `DBState`, then call `patchServerBackedSettings`, which fans out through `patchSettingsGroup`. |
| `src/ts/server/characterBridge.svelte.ts:37`, `:148` | `createServerBackedCharacterDraft`, `watchServerBackedCharacterProfile` | Server-backed character profile draft/watch helpers. | Writes character profile fields. | `PATCH /api/v1/commands/characters/:characterId`. | Pass | The draft seeds selected character fields, `watchServerBackedCharacterProfile` diffs scalar profile fields, and `runPendingCharacterPatch` calls `dispatchUpdateCharacter`; the server patch route validates a character patch and writes the character row. |
| `src/ts/chatCommands.ts` listed helpers | Chat/message command helpers. | Shared chat, folder, generation setting, script state, and message mutation helpers. | Writes chats, folders, generation settings, script state, and messages. | `server/fastify/src/routes/commands.ts:3520` through `:4499`. | Pass | The referenced command family exists: chat create/patch/delete/reorder, folder, generation-settings, script-state, message append/update/delete/truncate/replace/tail, and generation-result routes all require auth and mutate targeted chat/message storage. |
| `src/ts/globalApi.svelte.ts:159`, `src/ts/server/assets.ts:57` | `saveAsset`, `uploadServerAssetBytes` | Shared asset upload helper. | Stores asset bytes and metadata. | `POST /api/v1/assets`, `/api/v1/assets/bulk`. | Pass | `saveAsset` delegates to `uploadServerAsset`; `uploadServerAssetBytes` posts bytes with content type/auth and advances the command revision; server asset routes add bytes/metadata and return asset ids. |
| `src/ts/server/backups.ts:57`, `src/ts/server/realmImport.ts:31`, `src/ts/characters.ts` import helpers | Backup/import helpers. | Shared backup, restore, Realm import, and character/chat import helpers. | Imports/replaces app state, imports Realm characters, creates/restores/deletes backups. | `save.ts`, `realmImport.ts`, `backups.ts`. | Pass | Backup helpers call `/api/v1/backups` and import bundle routes; Realm helper posts to `/api/v1/import/realm-character` with `baseRevision`; save/import routes apply imported databases/assets and emit events. |
| `src/ts/process/request/serverChat.ts:323` | Server chat generation request. | Server-backed chat generation submit/cancel path. | Persists generation-side transcript/script mutations and final assistant messages; cancels jobs. | `generationChat.ts:2046`, `:2119`; result command `commands.ts:4499`. | Pass | The generation route accepts durable persisting modes, starts durable jobs, and cancel deletes `/api/v1/generate/chat/:id`; generation result persistence is backed by the `chats/:chatId/generation-result` command route. |
| `src/ts/process/request/serverMemory.ts:189` | Server memory job helpers. | Memory job request and cancel helpers. | Creates or cancels server memory jobs. | `memoryJobs.ts:81`, `:163`. | Pass | Client memory helpers call `/api/v1/memory/jobs` and `/api/v1/memory/jobs/:id`; server routes validate job creation, create repository rows, and cancel cancellable jobs. |

## Server Command Families

| Source | Unique id | Control | Database change | Server handling | Verify | Reason |
| --- | --- | --- | --- | --- | --- | --- |
| `server/fastify/src/routes/commands.ts:1319` | Settings | Settings command family. | Writes settings groups. | `PATCH /api/v1/commands/settings/:group`. | Pass | Route is present, auth-gated, validates grouped keys, writes settings, and co-writes `hypaV3Presets` when present. |
| `server/fastify/src/routes/commands.ts:1363`, `:1406`, `:1451`, `:1533`, `:1586`, `:1644`, `:1685`, `:2305` | Legacy bot presets | Legacy bot preset command family. | Writes legacy preset rows and extraction/conversion state. | Legacy bot preset handlers. | Pass | Legacy preset handlers exist for create/update/delete/copy/import/extract-style operations and are command-backed. |
| `server/fastify/src/routes/commands.ts:1739`, `:1778`, `:1829`, `:1891`, `:1933`, `:1972` | Model presets | Model preset command family. | Creates, patches, deletes, selects, imports, and reorders model presets. | Model preset handlers. | Pass | Model preset create/update/delete/select/import/reorder handlers are present and emit command events. |
| `server/fastify/src/routes/commands.ts:2024`, `:2063`, `:2112`, `:2173`, `:2214`, `:2253`, `:2349`, `:2381`, `:2419`, `:2460`, `:2496`, `:2541` | Prompt presets/settings/items | Prompt preset, prompt settings, and template item command family. | Writes prompt presets, prompt settings, and prompt template item rows. | Prompt preset/settings/item handlers. | Pass | Prompt preset create/update/delete/select/import/reorder plus prompt settings and item create/update/delete/enable/reorder handlers are present. |
| `server/fastify/src/routes/commands.ts:2580` through `:2813` | Personas | Persona command family. | Creates, patches, deletes, selects, reorders personas. | Persona handlers. | Pass | Handlers exist for create, patch, delete, select, and reorder operations and update collection/selection state. |
| `server/fastify/src/routes/commands.ts:2867` through `:3021` | Translator presets | Translator preset command family. | Creates, patches, deletes, selects translator presets. | Translator preset handlers. | Pass | Handlers exist for create/import, patch/rename, delete, and select with command events. |
| `server/fastify/src/routes/commands.ts:3059` through `:3222` | Loadouts | Loadout command family. | Creates, updates, deletes, favorites, touches loadouts. | Loadout handlers. | Pass | Route family exists for the documented loadout persistence operations. |
| `server/fastify/src/routes/commands.ts:3267` through `:3477` | Characters and order | Character/order command family. | Creates/imports/patches/deletes/selects/reorders characters. | Character handlers. | Pass | Routes cover character creation/import, profile patch, delete, select, and order mutation, with asset references validated on patch. |
| `server/fastify/src/routes/commands.ts:3520` through `:4126` | Chats/folders/generation settings | Chat, folder, generation settings, script state commands. | Writes chat metadata, folders, generation settings, script state. | Chat/folder handlers. | Pass | Routes exist for chat create/patch/settings/delete/fork/reorder, folder create/patch/delete/reorder, and script-state updates. |
| `server/fastify/src/routes/commands.ts:4180` through `:4499` | Messages/generation result | Message command family. | Appends, updates, deletes, truncates, replaces messages and stores generation result. | Message handlers. | Pass | Routes exist and write through targeted message storage, including duplicate-id validation and chat-location checks. |
| `server/fastify/src/routes/commands.ts:4559` through `:5243`, `:5855` through `:5997` | Lorebooks | Global, character, chat, and module lorebook command families. | Writes lorebook collections, entries, folders, order. | Lorebook handlers. | Pass | Global/character/chat/module lorebook routes are present for replace, entry update/delete, and reorder operations. |
| `server/fastify/src/routes/commands.ts:5284` through `:5473` | Modules | Module command family. | Creates, patches, deletes, enables, reorders/assigns modules. | Module handlers. | Pass | Module create/update/delete/enable and character module reorder routes exist. |
| `server/fastify/src/routes/commands.ts:5517` through `:5816` | Plugins and plugin storage | Plugin command family. | Imports, patches, deletes, enables plugins and writes plugin storage. | Plugin handlers. | Pass | Plugin import/update/delete/enable/current-provider and custom storage routes exist. |
| `server/fastify/src/routes/commands.ts:6039` through `:6155` | Scripts/triggers | Script and trigger command family. | Replaces character/module scripts and triggers. | Script/trigger handlers. | Pass | Character and module script/trigger replacement routes are present and command-backed. |
